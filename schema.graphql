scalar Date

schema {
  query: Query
}

type Query {
  findOrders: [Order]
}

enum Status {
  PENDING
  SHIPPED
  COMPLETE
}

interface Node {
  id: ID!
}

type Order implements Node {
  id: ID!
  customerFullName: String!
  totalPrice: Int!
  status: Status!
  createdDate: Date!
}
