query findOrders($statuses: [Status!]!) {
  findOrders(statuses: $statuses) {
    ...DashboardFields
  }
}

fragment DashboardFields on Order {
  id
  totalPrice
  status
  createdDate
  customer {
    fullName
  }
}
